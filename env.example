# Airflow Configuration
# Copy this file to .env and update with your values

# Fernet Key for Airflow encryption
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Or use: docker run --rm apache/airflow:2.9.1-python3.10 python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW__CORE__FERNET_KEY=

# Database Configuration (optional - defaults are used if not set)
# DB_USER=airflow
# DB_PASSWORD=airflow
# DB_HOST=postgres
# DB_PORT=5432
# DB_NAME=ny_taxi

# Airflow User ID (optional)
# AIRFLOW_UID=50000

